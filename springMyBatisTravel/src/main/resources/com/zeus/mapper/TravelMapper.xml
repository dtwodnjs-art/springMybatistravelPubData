<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.mapper.TravelMapper">

	<select id="list" resultType="com.zeus.domain.Travel">
		SELECT tno, title, destination, trip_date as tripDate, regdate, moddate
		FROM travel_info
		ORDER BY tno DESC
	</select>

	<insert id="create" parameterType="com.zeus.domain.Travel">
		<selectKey keyProperty="tno" resultType="int" order="BEFORE">
			SELECT seq_travel.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO travel_info (tno, title, destination, trip_date)
		VALUES (#{tno}, #{title}, #{destination}, #{tripDate})
	</insert>

	<insert id="createDetail"
		parameterType="com.zeus.domain.TravelDetail">
		INSERT INTO travel_detail (tno, transport, description)
		VALUES (#{tno}, #{transport}, #{description})
	</insert>

</mapper>